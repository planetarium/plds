{
  "name": "@plds/icons",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "optimize": "rimraf ./optimized & svgo -q -p 8 -f ./raw -o ./optimized",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.1",
    "svgo": "^3.0.2"
  }
}
